/**
 * Foo function description.
 * @function foo
 * @memberof stargatejs-device
 * @param {string} name name to log
 * @return {string} foo + name
 */

module.exports = function(name){
    return 'foo ' + name;
};